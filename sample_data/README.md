# Sample Data Directory

Place sample images here for training the image model.

Supported formats: `.jpg`, `.jpeg`, `.png`

The training script (`train_image.py`) will automatically use images from this directory.

For the MVP, you can use any images - the model will be trained to classify them as safe (label 0) by default.

